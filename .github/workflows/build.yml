name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify Docker is available
        run: docker --version

      - name: Placeholder build step
        run: echo "Service Dockerfiles will be built in later phases."
